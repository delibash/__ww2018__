import styled from 'styled-components';

const StyledContainer = styled.ul``;

const StyledItem = styled.li``;

const StyledItemHeader = styled.section`
  display: flex;
  margin: 1.8rem 0;
  justify-content: space-between;
  align-items: center;
`;

export {
  StyledContainer,
  StyledItem,
  StyledItemHeader,
};